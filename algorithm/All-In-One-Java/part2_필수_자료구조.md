# Part 2. 필수 자료구조

> **키워드**: `Array` `ArrayList` `LinkedList` `Stack` `Queue` `Deque` `HashMap` `HashSet` `시간 복잡도 비교`

---

## 📌 목차

1. [Array (배열)](#1-array-배열)
2. [ArrayList](#2-arraylist)
3. [Array vs ArrayList 비교](#3-array-vs-arraylist-비교)
4. [LinkedList (연결 리스트)](#4-linkedlist-연결-리스트)
5. [ArrayList vs LinkedList 비교](#5-arraylist-vs-linkedlist-비교)
6. [Stack (스택)](#6-stack-스택)
7. [Queue (큐)](#7-queue-큐)
8. [Deque (덱) — 스택과 큐의 만능 대체재](#8-deque-덱--스택과-큐의-만능-대체재)
9. [HashMap (해시 테이블)](#9-hashmap-해시-테이블)
10. [HashSet (해시 셋)](#10-hashset-해시-셋)
11. [HashMap vs HashSet 비교](#11-hashmap-vs-hashset-비교)
12. [자료구조 선택 가이드](#12-자료구조-선택-가이드)
13. [전체 시간 복잡도 비교표](#13-전체-시간-복잡도-비교표)
14. [QUIZ](#quiz)

---

## 1. Array (배열)

### 한 줄 정의

**같은 타입**의 데이터를 **연속된 메모리 공간**에 **고정된 크기**로 저장하는 자료구조다.

### 개념

배열은 가장 기본적인 자료구조다. 메모리에 데이터가 연속으로 나란히 저장되기 때문에 **인덱스를 통한 접근이 O(1)**로 매우 빠르다. 하지만 한번 크기를 정하면 **늘리거나 줄일 수 없다.**

```
메모리 구조 (int[] arr = {10, 20, 30, 40, 50})

주소:    [100] [104] [108] [112] [116]
값:       10    20    30    40    50
인덱스:   [0]   [1]   [2]   [3]   [4]

→ 각 int는 4바이트, 시작 주소만 알면 인덱스 × 4를 더해서 바로 접근 가능
→ arr[3]의 주소 = 100 + (3 × 4) = 112 → O(1)
```

### 선언과 사용

```java
// 1. 크기만 지정 (기본값 0으로 초기화)
int[] arr = new int[5];          // [0, 0, 0, 0, 0]

// 2. 값을 직접 지정
int[] arr = {10, 20, 30, 40, 50};

// 3. 2차원 배열
int[][] matrix = new int[3][4];  // 3행 4열
int[][] matrix = {
    {1, 2, 3},
    {4, 5, 6},
    {7, 8, 9}
};

// 접근
arr[0] = 100;                    // O(1) — 인덱스로 바로 접근
int len = arr.length;            // 배열 길이

// 순회
for (int i = 0; i < arr.length; i++) {
    System.out.println(arr[i]);
}

// for-each (인덱스가 필요 없을 때)
for (int num : arr) {
    System.out.println(num);
}
```

### 배열의 한계

```java
int[] arr = new int[3];  // 크기 3으로 고정

arr[0] = 10;
arr[1] = 20;
arr[2] = 30;
arr[3] = 40;  // 💥 ArrayIndexOutOfBoundsException!

// 중간 삽입/삭제도 불편하다
// arr = [10, 20, 30, 40, 50] 에서 인덱스 1의 20을 삭제하려면?
// → 뒤의 원소를 하나씩 앞으로 당겨야 한다 → O(n)
```

### 시간 복잡도

| 연산 | 복잡도 | 설명 |
|------|--------|------|
| 인덱스 접근 `arr[i]` | **O(1)** | 주소 계산으로 바로 접근 |
| 값 수정 `arr[i] = x` | **O(1)** | 위치를 알고 있으니 바로 수정 |
| 맨 뒤 삽입 | **O(1)** | 빈 공간이 있다면 |
| 중간 삽입/삭제 | **O(n)** | 뒤의 원소를 전부 이동시켜야 함 |
| 값 검색 (비정렬) | **O(n)** | 처음부터 하나씩 확인 |
| 값 검색 (정렬됨) | **O(log n)** | 이진 탐색 가능 |

### 코딩테스트에서 자주 쓰는 배열 메서드

```java
import java.util.Arrays;

int[] arr = {5, 3, 1, 4, 2};

// 정렬
Arrays.sort(arr);                    // [1, 2, 3, 4, 5] — O(n log n)

// 부분 정렬
Arrays.sort(arr, 0, 3);             // 인덱스 0~2만 정렬

// 배열 채우기
Arrays.fill(arr, 0);                 // 모든 원소를 0으로 — O(n)

// 배열 복사
int[] copy = Arrays.copyOf(arr, arr.length);         // 전체 복사
int[] partial = Arrays.copyOfRange(arr, 1, 4);       // 인덱스 1~3 복사

// 이진 탐색 (정렬된 배열에서만!)
Arrays.sort(arr);
int idx = Arrays.binarySearch(arr, 3);  // 값 3의 인덱스 반환 — O(log n)

// 배열 → 문자열 (디버깅용)
System.out.println(Arrays.toString(arr));     // [1, 2, 3, 4, 5]
System.out.println(Arrays.deepToString(matrix)); // 2차원 배열용
```

---

## 2. ArrayList

### 한 줄 정의

**크기가 자동으로 늘어나는 배열**이다. 내부적으로 배열을 사용하지만, 공간이 부족하면 더 큰 배열을 만들어 복사한다.

### 개념

Java의 `ArrayList`는 내부에 `Object[]` 배열을 가지고 있다. 초기 용량(기본 10)을 넘으면 **기존 크기의 약 1.5배** 크기의 새 배열을 만들고 기존 데이터를 복사한다.

```
ArrayList 내부 동작:

1. 초기 상태 (capacity=4)
   [10] [20] [30] [  ]       ← 3개 저장, 1칸 남음

2. add(40) — 빈 자리에 추가 → O(1)
   [10] [20] [30] [40]       ← 꽉 참!

3. add(50) — 공간 부족! → 새 배열(capacity=6) 만들고 복사 → O(n)
   [10] [20] [30] [40] [50] [  ]
```

### 선언과 사용

```java
import java.util.ArrayList;
import java.util.List;
import java.util.Collections;

// 선언 (인터페이스 타입으로 선언하는 것이 관례)
List<Integer> list = new ArrayList<>();

// 추가
list.add(10);           // 맨 뒤에 추가 — O(1) (평균)
list.add(20);
list.add(30);           // [10, 20, 30]

list.add(1, 15);        // 인덱스 1에 삽입 — O(n) (뒤로 밀어야 함)
                        // [10, 15, 20, 30]

// 접근
int val = list.get(0);  // 인덱스 접근 — O(1)

// 수정
list.set(0, 100);       // 인덱스 0의 값을 100으로 — O(1)

// 삭제
list.remove(2);         // 인덱스 2 삭제 — O(n) (앞으로 당겨야 함)

// ⚠️ 주의: Integer 값으로 삭제하려면 객체로 감싸야 함
list.remove(Integer.valueOf(20));  // 값이 20인 첫 번째 원소 삭제

// 크기
int size = list.size();

// 포함 여부 확인
boolean has = list.contains(10);   // O(n) — 처음부터 끝까지 탐색

// 비어있는지
boolean empty = list.isEmpty();

// 정렬
Collections.sort(list);            // 오름차순 — O(n log n)
Collections.sort(list, Collections.reverseOrder()); // 내림차순

// 순회
for (int i = 0; i < list.size(); i++) {
    System.out.println(list.get(i));
}

for (int num : list) {
    System.out.println(num);
}
```

### 시간 복잡도

| 연산 | 복잡도 | 설명 |
|------|--------|------|
| `get(i)` | **O(1)** | 내부가 배열이니까 인덱스 접근 빠름 |
| `set(i, val)` | **O(1)** | 마찬가지 |
| `add(val)` (맨 뒤) | **O(1)** 평균 | 가끔 배열 확장 시 O(n) |
| `add(i, val)` (중간) | **O(n)** | 뒤의 원소를 밀어야 함 |
| `remove(i)` | **O(n)** | 뒤의 원소를 당겨야 함 |
| `contains(val)` | **O(n)** | 처음부터 하나씩 비교 |

---

## 3. Array vs ArrayList 비교

| 항목 | Array | ArrayList |
|------|-------|-----------|
| 크기 | **고정** (선언 시 결정) | **가변** (자동 확장) |
| 타입 | 기본형 가능 (`int[]`) | 래퍼 타입만 (`Integer`) |
| 성능 | 약간 더 빠름 (오버헤드 없음) | 약간 느림 (객체 래핑, 크기 확장) |
| 편의성 | 메서드 적음 | `add`, `remove`, `contains` 등 풍부 |
| 메모리 | 더 효율적 | 객체 래핑 + 여유 공간으로 더 사용 |
| 다차원 | `int[][]` 쉽게 가능 | `List<List<Integer>>` 번거로움 |

### 언제 뭘 쓸까?

```java
// Array — 크기가 정해져 있고, 기본형이며, 성능이 중요할 때
int[] dp = new int[n + 1];           // DP 테이블
int[][] graph = new int[n][n];       // 인접 행렬
boolean[] visited = new boolean[n];  // 방문 배열

// ArrayList — 크기를 모르거나, 삽입/삭제가 잦을 때
List<Integer> result = new ArrayList<>();  // 결과를 모을 때
List<int[]> edges = new ArrayList<>();     // 간선 목록
```

> 💡 **코딩테스트 실전**: 크기가 정해진 경우 `int[]` 배열이 `ArrayList<Integer>`보다 빠르다. DP, 방문 배열, 인접 행렬 등에는 기본 배열을 쓰자.

---

## 4. LinkedList (연결 리스트)

### 한 줄 정의

각 데이터(노드)가 **다음 노드의 주소(참조)**를 가지고 있어서 체인처럼 연결된 자료구조다.

### 개념

배열과 달리 메모리에 **연속으로 저장되지 않는다.** 각 노드가 `데이터 + 다음 노드의 참조`를 가진다. Java의 `LinkedList`는 **이중 연결 리스트(Doubly Linked List)**로, 이전 노드의 참조도 가지고 있다.

```
단일 연결 리스트 (Singly Linked List):

  [10|→] → [20|→] → [30|→] → [40|→] → null
  head                                  tail

이중 연결 리스트 (Doubly Linked List — Java의 LinkedList):

  null ← [←|10|→] ⇄ [←|20|→] ⇄ [←|30|→] ⇄ [←|40|→] → null
          head                                  tail
```

### 선언과 사용

```java
import java.util.LinkedList;
import java.util.List;

// List 인터페이스로 선언 (ArrayList와 교체 가능)
List<Integer> list = new LinkedList<>();

// 추가
list.add(10);           // 맨 뒤에 추가 — O(1)
list.add(20);
list.add(30);           // [10, 20, 30]

list.add(1, 15);        // 인덱스 1에 삽입 — O(n) (탐색) + O(1) (삽입)
                        // [10, 15, 20, 30]

// 접근
int val = list.get(2);  // 인덱스 접근 — O(n) ⚠️ 느리다!

// 삭제
list.remove(0);         // 맨 앞 삭제 — O(1) ← ArrayList와의 핵심 차이!
```

### LinkedList 전용 메서드 (Deque 인터페이스)

```java
// LinkedList를 LinkedList 타입으로 선언하면 추가 메서드 사용 가능
LinkedList<Integer> ll = new LinkedList<>();

// 맨 앞/뒤 조작 — 모두 O(1)
ll.addFirst(10);       // 맨 앞에 추가
ll.addLast(20);        // 맨 뒤에 추가
ll.removeFirst();      // 맨 앞 삭제
ll.removeLast();       // 맨 뒤 삭제
ll.getFirst();         // 맨 앞 조회
ll.getLast();          // 맨 뒤 조회

ll.peek();             // 맨 앞 조회 (비어있으면 null)
ll.poll();             // 맨 앞 제거 + 반환 (비어있으면 null)
ll.offer(30);          // 맨 뒤에 추가
```

### 시간 복잡도

| 연산 | 복잡도 | 설명 |
|------|--------|------|
| `get(i)` | **O(n)** | head부터 i번째까지 순회해야 함 |
| `add(val)` (맨 뒤) | **O(1)** | tail 포인터로 바로 추가 |
| `addFirst(val)` | **O(1)** | head 앞에 노드 연결 |
| `remove(0)` (맨 앞) | **O(1)** | head 포인터만 변경 |
| `remove(i)` (중간) | **O(n)** | i번째까지 순회 후 삭제 |
| `contains(val)` | **O(n)** | 처음부터 순회 |

---

## 5. ArrayList vs LinkedList 비교

| 항목 | ArrayList | LinkedList |
|------|-----------|------------|
| 내부 구조 | **배열** (연속 메모리) | **노드 + 포인터** (분산 메모리) |
| 인덱스 접근 `get(i)` | **O(1)** ✅ | **O(n)** ❌ |
| 맨 뒤 추가 | **O(1)** 평균 | **O(1)** |
| **맨 앞 삽입/삭제** | **O(n)** ❌ | **O(1)** ✅ |
| 중간 삽입/삭제 | O(n) (원소 이동) | O(n) (탐색) + O(1) (삽입) |
| 메모리 | 효율적 (데이터만 저장) | 비효율 (포인터 추가 저장) |
| 캐시 성능 | 좋음 (연속 메모리) | 나쁨 (분산 메모리) |

### 결론

```
✅ 대부분의 경우 ArrayList가 유리하다!
   → 인덱스 접근이 O(1)이고 캐시 친화적

✅ LinkedList가 유리한 경우:
   → 맨 앞에서 삽입/삭제가 빈번한 경우 (Queue로 사용할 때)
   → 하지만 이 경우에도 ArrayDeque가 더 빠르다 (아래에서 설명)
```

> ⚠️ **코딩테스트 실전**: ArrayList의 `remove(0)`은 O(n)이므로, 큐처럼 맨 앞에서 빼는 동작이 필요하면 `ArrayDeque`나 `LinkedList`를 큐로 사용하자.

---

## 6. Stack (스택)

### 한 줄 정의

**LIFO (Last In First Out)** — 마지막에 넣은 것을 먼저 꺼내는 자료구조다. 접시 쌓기와 같다.

### 개념

```
push(10) → push(20) → push(30) → pop() → pop()

  |    |    |    |    | 30 |    | 20 |    |    |
  |    |    | 20 |    | 20 |    | 20 |    |    |
  | 10 |    | 10 |    | 10 |    | 10 |    | 10 |
  +----+    +----+    +----+    +----+    +----+
             ↑ push     ↑ push   ↑ pop     ↑ pop
                                 30 나옴    20 나옴
```

### 선언과 사용

```java
import java.util.Stack;
import java.util.ArrayDeque;
import java.util.Deque;

// 방법 1: Stack 클래스 (레거시 — 동기화 오버헤드 있음)
Stack<Integer> stack1 = new Stack<>();

// 방법 2: ArrayDeque (권장 — 더 빠름)
Deque<Integer> stack = new ArrayDeque<>();

// 삽입
stack.push(10);     // O(1)
stack.push(20);
stack.push(30);     // [10, 20, 30] ← 30이 top

// 삭제 + 반환
int top = stack.pop();     // 30 반환, [10, 20] — O(1)

// 조회 (삭제하지 않고 top만 확인)
int peek = stack.peek();   // 20 반환, [10, 20] 그대로 — O(1)

// 비어있는지 확인
boolean empty = stack.isEmpty();   // false

// 크기
int size = stack.size();           // 2
```

### Stack vs ArrayDeque

| 항목 | Stack | ArrayDeque |
|------|-------|------------|
| 상속 | `Vector` (synchronized) | `AbstractCollection` |
| 동기화 | **있음** (느림) | **없음** (빠름) |
| 성능 | 느림 | **빠름** ✅ |
| Java 공식 권장 | ❌ | ✅ |

> 💡 `Stack` 클래스는 `Vector`를 상속받아서 모든 메서드에 `synchronized`가 걸려있다. 멀티스레드가 아닌 코딩테스트에서는 불필요한 오버헤드다. **`ArrayDeque`를 스택으로 쓰자.**

### 스택이 쓰이는 대표 문제

| 문제 유형 | 설명 |
|----------|------|
| **괄호 검사** | `({[]})` 같은 문자열이 올바른지 확인 |
| **후위 표기법 계산** | `3 4 + 2 ×` 같은 수식 계산 |
| **DFS (깊이 우선 탐색)** | 재귀 대신 스택으로 구현 |
| **뒤로 가기** | 브라우저 히스토리 |
| **모노톤 스택** | 다음 큰 원소 찾기 등 |

### 예제: 괄호 검사

```java
public boolean isValid(String s) {
    Deque<Character> stack = new ArrayDeque<>();

    for (char c : s.toCharArray()) {
        // 여는 괄호면 push
        if (c == '(' || c == '{' || c == '[') {
            stack.push(c);
        }
        // 닫는 괄호면 pop해서 짝이 맞는지 확인
        else {
            if (stack.isEmpty()) return false;

            char top = stack.pop();
            if (c == ')' && top != '(') return false;
            if (c == '}' && top != '{') return false;
            if (c == ']' && top != '[') return false;
        }
    }

    return stack.isEmpty(); // 스택이 비어야 모든 괄호가 매칭된 것
}

// isValid("({[]})")  → true
// isValid("({[}])")  → false
// isValid("((")      → false
```

---

## 7. Queue (큐)

### 한 줄 정의

**FIFO (First In First Out)** — 먼저 넣은 것을 먼저 꺼내는 자료구조다. 줄 서기와 같다.

### 개념

```
offer(10) → offer(20) → offer(30) → poll() → poll()

Front                    Rear
  ↓                        ↓
 [10]                              → 10이 먼저 나옴
 [10] [20]
 [10] [20] [30]
      [20] [30]          ← poll()로 10 제거
           [30]          ← poll()로 20 제거
```

### 선언과 사용

```java
import java.util.Queue;
import java.util.LinkedList;
import java.util.ArrayDeque;

// 방법 1: LinkedList로 구현
Queue<Integer> queue1 = new LinkedList<>();

// 방법 2: ArrayDeque로 구현 (권장 — 더 빠름)
Queue<Integer> queue = new ArrayDeque<>();

// 삽입 (뒤에 추가)
queue.offer(10);    // O(1)
queue.offer(20);
queue.offer(30);    // [10, 20, 30] ← front: 10, rear: 30

// 삭제 + 반환 (앞에서 제거)
int front = queue.poll();   // 10 반환, [20, 30] — O(1)

// 조회 (삭제하지 않고 front만 확인)
int peek = queue.peek();    // 20 반환, [20, 30] 그대로 — O(1)

// 비어있는지 확인
boolean empty = queue.isEmpty();

// 크기
int size = queue.size();
```

### add/remove vs offer/poll 차이

| 상황 | 예외 발생 메서드 | null 반환 메서드 (권장) |
|------|-----------------|---------------------|
| 삽입 | `add()` → 실패 시 **예외** | `offer()` → 실패 시 **false** |
| 삭제 | `remove()` → 비어있으면 **예외** | `poll()` → 비어있으면 **null** |
| 조회 | `element()` → 비어있으면 **예외** | `peek()` → 비어있으면 **null** |

> 💡 **코딩테스트에서는 `offer/poll/peek`을 쓰자.** 예외 터지면 디버깅이 힘들다.

### 시간 복잡도

| 연산 | 복잡도 | 설명 |
|------|--------|------|
| `offer(val)` | **O(1)** | 뒤에 추가 |
| `poll()` | **O(1)** | 앞에서 제거 |
| `peek()` | **O(1)** | 앞의 원소 조회 |
| `contains(val)` | **O(n)** | 전체 순회 |

### 큐가 쓰이는 대표 문제

| 문제 유형 | 설명 |
|----------|------|
| **BFS (너비 우선 탐색)** | 그래프/트리 레벨 순회 |
| **최단 경로 (가중치 없음)** | BFS로 풀 수 있는 미로 탐색 등 |
| **프로세스 스케줄링** | 작업 순서 관리 |
| **캐시 (LRU)** | 가장 오래된 항목부터 제거 |

### 예제: BFS (너비 우선 탐색)

```java
// 그래프에서 시작 노드로부터 모든 노드까지의 최단 거리
public int[] bfs(List<List<Integer>> graph, int start) {
    int n = graph.size();
    int[] dist = new int[n];
    Arrays.fill(dist, -1);             // -1 = 미방문

    Queue<Integer> queue = new ArrayDeque<>();
    queue.offer(start);
    dist[start] = 0;

    while (!queue.isEmpty()) {
        int curr = queue.poll();        // 앞에서 꺼냄

        for (int next : graph.get(curr)) {
            if (dist[next] == -1) {     // 미방문이면
                dist[next] = dist[curr] + 1;
                queue.offer(next);      // 뒤에 추가
            }
        }
    }

    return dist;
}
```

---

## 8. Deque (덱) — 스택과 큐의 만능 대체재

### 한 줄 정의

**Double-Ended Queue** — 양쪽 끝에서 삽입과 삭제가 모두 가능한 자료구조다.

### 개념

```
        offerFirst()                    offerLast()
            ↓                               ↓
Front  [  ] [10] [20] [30] [40] [  ]  Rear
            ↑                               ↑
        pollFirst()                     pollLast()
```

Deque는 **스택으로도, 큐로도** 사용할 수 있는 만능 자료구조다.

### 선언과 사용

```java
import java.util.Deque;
import java.util.ArrayDeque;

Deque<Integer> deque = new ArrayDeque<>();

// === 큐처럼 사용 (FIFO) ===
deque.offerLast(10);       // 뒤에 추가 (= offer)
deque.offerLast(20);
deque.offerLast(30);       // [10, 20, 30]
deque.pollFirst();         // 10 반환 (= poll) — 앞에서 꺼냄

// === 스택처럼 사용 (LIFO) ===
deque.push(10);            // 앞에 추가 (= offerFirst)
deque.push(20);
deque.push(30);            // [30, 20, 10]
deque.pop();               // 30 반환 (= pollFirst) — 앞에서 꺼냄

// === 양쪽 모두 사용 ===
deque.offerFirst(1);       // 앞에 추가
deque.offerLast(100);      // 뒤에 추가
deque.peekFirst();         // 앞 조회
deque.peekLast();          // 뒤 조회
deque.pollFirst();         // 앞에서 제거
deque.pollLast();          // 뒤에서 제거
```

### Deque 메서드 정리

| 위치 | 삽입 | 삭제 | 조회 |
|------|------|------|------|
| **앞 (Front)** | `offerFirst()` | `pollFirst()` | `peekFirst()` |
| **뒤 (Rear)** | `offerLast()` | `pollLast()` | `peekLast()` |
| **스택 별칭** | `push()` = offerFirst | `pop()` = pollFirst | `peek()` = peekFirst |
| **큐 별칭** | `offer()` = offerLast | `poll()` = pollFirst | `peek()` = peekFirst |

### 왜 ArrayDeque가 권장되는가?

| 항목 | Stack | LinkedList (Queue) | ArrayDeque |
|------|-------|-------------------|------------|
| 스택 용도 | ✅ 가능 | ✅ 가능 | ✅ **권장** |
| 큐 용도 | ❌ | ✅ 가능 | ✅ **권장** |
| 내부 구조 | 배열 (동기화) | 노드 + 포인터 | **원형 배열** |
| 성능 | 느림 (동기화) | 보통 (포인터 오버헤드) | **가장 빠름** |
| 메모리 | 보통 | 많음 (노드마다 포인터) | **효율적** |
| null 허용 | ✅ | ✅ | ❌ |

> 💡 **결론**: 코딩테스트에서 스택이 필요하면 `Deque<T> stack = new ArrayDeque<>()`, 큐가 필요하면 `Queue<T> queue = new ArrayDeque<>()`를 쓰자. `ArrayDeque`가 만능이다.

---

## 9. HashMap (해시 테이블)

### 한 줄 정의

**Key-Value 쌍**으로 데이터를 저장하며, Key를 통한 검색/삽입/삭제가 평균 **O(1)**인 자료구조다.

### 개념

HashMap은 내부적으로 **배열 + 해시 함수**를 사용한다. Key를 해시 함수에 넣으면 배열의 인덱스가 나오고, 그 위치에 Value를 저장한다.

```
해시 함수 동작:

Key "apple"  → hashCode() → 97  → 97 % 16 = 1  → bucket[1]에 저장
Key "banana" → hashCode() → 205 → 205 % 16 = 13 → bucket[13]에 저장
Key "cherry" → hashCode() → 143 → 143 % 16 = 15 → bucket[15]에 저장

bucket 배열:
[0]  → null
[1]  → ("apple", 3000)
[2]  → null
...
[13] → ("banana", 2000)
...
[15] → ("cherry", 5000)
```

### 해시 충돌 (Hash Collision)

서로 다른 Key가 같은 인덱스에 매핑될 수 있다. 이를 **해시 충돌**이라 한다.

```
"apple"  → hash % 16 = 1
"melon"  → hash % 16 = 1  ← 같은 인덱스! 충돌!

해결: Chaining (연결 리스트로 이어붙이기)
bucket[1] → ("apple", 3000) → ("melon", 4000)

→ 충돌이 많아지면 O(1) → O(n)으로 성능 저하
→ Java 8부터는 충돌이 8개 이상이면 연결 리스트 → 레드-블랙 트리로 전환 (O(log n))
```

### 선언과 사용

```java
import java.util.HashMap;
import java.util.Map;

Map<String, Integer> map = new HashMap<>();

// === 삽입 ===
map.put("apple", 3000);     // O(1) 평균
map.put("banana", 2000);
map.put("cherry", 5000);

// 같은 Key로 put하면 Value가 덮어써진다
map.put("apple", 3500);     // apple: 3000 → 3500

// === 조회 ===
int price = map.get("apple");          // 3500 — O(1)
int def = map.getOrDefault("grape", 0); // 없으면 기본값 0 반환

// === 존재 여부 확인 ===
boolean hasKey = map.containsKey("banana");    // true — O(1)
boolean hasVal = map.containsValue(2000);      // true — O(n) ⚠️

// === 삭제 ===
map.remove("cherry");       // O(1) 평균

// === 크기 ===
int size = map.size();      // 2

// === 순회 ===
// 방법 1: entrySet (Key와 Value 모두 필요할 때 — 가장 효율적)
for (Map.Entry<String, Integer> entry : map.entrySet()) {
    System.out.println(entry.getKey() + ": " + entry.getValue());
}

// 방법 2: keySet (Key만 필요하거나 Key로 Value 조회)
for (String key : map.keySet()) {
    System.out.println(key + ": " + map.get(key));
}

// 방법 3: values (Value만 필요할 때)
for (int val : map.values()) {
    System.out.println(val);
}
```

### 코딩테스트 필수 패턴: 빈도수 세기

```java
// 문자열에서 각 문자의 등장 횟수 세기
public Map<Character, Integer> countChars(String s) {
    Map<Character, Integer> map = new HashMap<>();

    for (char c : s.toCharArray()) {
        // 방법 1: getOrDefault
        map.put(c, map.getOrDefault(c, 0) + 1);

        // 방법 2: merge (더 간결)
        // map.merge(c, 1, Integer::sum);
    }

    return map;
}

// countChars("banana")
// → {b=1, a=3, n=2}
```

### 코딩테스트 필수 패턴: 두 수의 합 (Two Sum)

```java
// 배열에서 합이 target이 되는 두 수의 인덱스 찾기
public int[] twoSum(int[] nums, int target) {
    Map<Integer, Integer> map = new HashMap<>();  // 값 → 인덱스

    for (int i = 0; i < nums.length; i++) {
        int complement = target - nums[i];

        if (map.containsKey(complement)) {        // O(1) 검색!
            return new int[]{map.get(complement), i};
        }

        map.put(nums[i], i);
    }

    return new int[]{};
}

// twoSum([2, 7, 11, 15], 9) → [0, 1]  (2 + 7 = 9)
// O(n) — HashMap 없이 이중 for문이면 O(n²)
```

### 시간 복잡도

| 연산 | 평균 | 최악 (해시 충돌) |
|------|------|---------------|
| `put(key, val)` | **O(1)** | O(n) |
| `get(key)` | **O(1)** | O(n) |
| `containsKey(key)` | **O(1)** | O(n) |
| `containsValue(val)` | **O(n)** | O(n) |
| `remove(key)` | **O(1)** | O(n) |

> ⚠️ **주의**: `containsValue()`는 모든 Value를 순회하므로 **O(n)**이다! Key 검색만 O(1).

---

## 10. HashSet (해시 셋)

### 한 줄 정의

**중복을 허용하지 않고**, 순서도 보장하지 않는 집합(Set) 자료구조다. 내부적으로 HashMap을 사용한다.

### 개념

HashSet은 사실 **HashMap에서 Key만 사용**하는 것이다. Value는 내부적으로 더미 값(`PRESENT`)이 들어간다.

```
HashSet<String> set에 "apple", "banana" 추가

내부 HashMap:
  "apple"  → PRESENT (더미 값)
  "banana" → PRESENT (더미 값)

→ 핵심은 Key의 존재 여부만 확인하는 것!
```

### 선언과 사용

```java
import java.util.HashSet;
import java.util.Set;

Set<Integer> set = new HashSet<>();

// === 삽입 ===
set.add(10);         // O(1)
set.add(20);
set.add(30);
set.add(10);         // 중복! 무시됨. set = {10, 20, 30}

// === 존재 여부 확인 ===
boolean has = set.contains(20);    // true — O(1) ← 핵심!

// === 삭제 ===
set.remove(20);      // O(1)

// === 크기 ===
int size = set.size();

// === 비어있는지 ===
boolean empty = set.isEmpty();

// === 순회 ===
for (int num : set) {
    System.out.println(num);  // 순서 보장 안 됨!
}

// === 배열 → Set 변환 ===
Set<String> fromArray = new HashSet<>(Arrays.asList("a", "b", "c"));

// === List → Set 변환 (중복 제거) ===
List<Integer> list = Arrays.asList(1, 2, 2, 3, 3, 3);
Set<Integer> unique = new HashSet<>(list);  // {1, 2, 3}
```

### 시간 복잡도

| 연산 | 평균 | 최악 |
|------|------|------|
| `add(val)` | **O(1)** | O(n) |
| `contains(val)` | **O(1)** | O(n) |
| `remove(val)` | **O(1)** | O(n) |

---

## 11. HashMap vs HashSet 비교

| 항목 | HashMap | HashSet |
|------|---------|---------|
| 저장 방식 | **Key-Value** 쌍 | **Key만** (중복 없는 집합) |
| 내부 구조 | 해시 테이블 | HashMap을 내부에 사용 |
| 중복 | Key 중복 ❌, Value 중복 ✅ | 원소 중복 ❌ |
| 용도 | 값 매핑 (사전, 카운팅) | 존재 여부 확인, 중복 제거 |
| 접근 | `get(key)` → Value 반환 | `contains(val)` → boolean 반환 |

### 언제 뭘 쓸까?

```java
// HashMap — Key에 대응하는 Value가 필요할 때
// "이 사람의 점수는?" "이 단어의 등장 횟수는?"
Map<String, Integer> scores = new HashMap<>();
scores.put("홍길동", 95);

// HashSet — 존재 여부만 확인하면 될 때
// "이 숫자가 있었나?" "중복이 있나?"
Set<Integer> seen = new HashSet<>();
seen.contains(42);
```

---

## 12. 자료구조 선택 가이드

문제를 읽고 아래 기준으로 자료구조를 선택하자:

| 상황 | 추천 자료구조 | 이유 |
|------|-------------|------|
| 크기가 정해져 있고, 인덱스 접근이 필요 | **Array** (`int[]`) | O(1) 접근, 메모리 효율적 |
| 크기를 모르고, 뒤에 추가가 잦음 | **ArrayList** | 동적 크기, O(1) 접근 |
| 앞/뒤 삽입 삭제가 잦음 | **ArrayDeque** | 양쪽 O(1) |
| LIFO (마지막 넣은 것 먼저) | **ArrayDeque** (스택) | push/pop O(1) |
| FIFO (처음 넣은 것 먼저) | **ArrayDeque** (큐) | offer/poll O(1) |
| Key로 Value를 빠르게 찾기 | **HashMap** | O(1) 검색 |
| 중복 제거 / 존재 여부 확인 | **HashSet** | O(1) contains |
| 빈도수 세기 | **HashMap** | getOrDefault 패턴 |
| 정렬된 순서가 필요 | **TreeMap / TreeSet** | O(log n), 자동 정렬 |
| 우선순위가 필요 | **PriorityQueue (힙)** | O(log n) 삽입/삭제 |

---

## 13. 전체 시간 복잡도 비교표

| 자료구조 | 접근 | 검색 | 삽입 (뒤) | 삽입 (앞) | 삭제 (뒤) | 삭제 (앞) |
|----------|------|------|----------|----------|----------|----------|
| **Array** | O(1) | O(n) | O(1)* | O(n) | O(1)* | O(n) |
| **ArrayList** | O(1) | O(n) | O(1) 평균 | O(n) | O(1) | O(n) |
| **LinkedList** | O(n) | O(n) | O(1) | O(1) | O(1) | O(1) |
| **ArrayDeque** | — | O(n) | O(1) | O(1) | O(1) | O(1) |
| **Stack** | — | O(n) | O(1) | — | O(1) | — |
| **Queue** | — | O(n) | O(1) | — | — | O(1) |
| **HashMap** | O(1)** | O(1)** | O(1)** | — | — | — |
| **HashSet** | — | O(1)** | O(1)** | — | — | — |

`*` 빈 공간이 있을 때 | `**` 평균, 최악 O(n)

---

## 📝 Part 2 핵심 정리

```
1. Array = 고정 크기, 연속 메모리, 인덱스 접근 O(1) — DP, 방문 배열 등에 사용
2. ArrayList = 동적 배열, 인덱스 접근 O(1), 중간 삽입/삭제 O(n) — 대부분의 리스트에 사용
3. LinkedList = 포인터 연결, 앞/뒤 삽입/삭제 O(1), 인덱스 접근 O(n) — 큐로 사용 가능하나 ArrayDeque가 더 빠름
4. Stack = LIFO, push/pop O(1) — 괄호 검사, DFS, 모노톤 스택
5. Queue = FIFO, offer/poll O(1) — BFS, 최단 경로
6. ArrayDeque = 스택 + 큐 만능 대체재, 코딩테스트에서 가장 많이 쓰는 자료구조
7. HashMap = Key-Value, 검색/삽입/삭제 O(1) — 빈도수 세기, Two Sum, 매핑
8. HashSet = 중복 없는 집합, contains O(1) — 중복 확인, 존재 여부
9. List.contains() O(n) vs Set.contains() O(1) → 빠른 검색은 반드시 Set!
10. 자료구조 선택이 곧 시간 복잡도를 결정한다!
```

---

> 📚 **다음 파트 예고**: 정렬 알고리즘 (Bubble, Selection, Insertion, Merge, Quick Sort) & 재귀
