# HTML

## 1. HTML이란 무엇인가요?
1989년 팀 버너스리는 전 세계 연구자가 정보를 쉽게 공유할 수 있도록 웹을 만들엇다.
당시에는 컴퓨터 시스템과 플랫폼마다 사용하는 문서 형식이 달라 불편했는데 이러한 호환성 문제를 해결하기 위해 설계한 마크업 언어가 HTML(HyperText Markup Language)입니다.

초기 HTML은 문서의 구조를 표현하는 데 집중하였다면, 요즘에는 웹이 발전하면서 각 요소의 의미를 명확하게 전달하는 것이 중요해졌고 이 과정에서 시멘틱HTML(semantic HTML)이라는 개념이 등장했다.

책의 답변
- HTML은 웹의 구조를 담당합니다. 디자인을 담당하는 CSS, 동적 기능을 담당하는 자바스크립트와 함께 현대적인 웹을 구성합니다. 특히 HTML은 각 요소의 의미와 구조를 명확하게 표현하여 검색 엔진이나 스크린 리더와 같은 접근성 도구가 내용을 정확히 이해할 수 있게 합니다. 그 덕분에 웹 문서가 서로 연결되고 더 잘 검색되며 누구나 접근하기 쉬운 웹 환경이 만들어 집니다.

책의 모의 면접 질문
1. 웹 개발에서 HTML, CSS, 자바스크립트의 역할은 무엇인가? 건물 짓기에 비유하면 각 어떤 부분에 해당할까요?
2. HTML에서 시맨틱 태그를 사용하는 이유는 무엇인가요? 이것이 검색 엔진 최적화(SEO)와 웹 접근성에 어떤 도움을 줄까요?


## 2. HTML의 Doctype은 어떤 역할을 하나요?

<!DOCTYPE>은 문서가 어떤 HTML 버전을 따르는지 웹 브라우저에 알려 주는 선언문.

- Doctype의 과거와 현재
    - HTML4와 XHTML 문서를 정의하는 Doctype 선언
        ```
        <!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "http://www.w3.org/TR/html4/loose.dtd">
        <!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.1//EN" "http://www.w3.org/TR/xhtml11/DTD/xhtml11.dtd">
        ```
    - HTML5 문서를 정의하는 Doctype 선언
        ```
        <!DOCTYPE html>
        ```
    

책의 답변
- <!DOCTYPE>은 문서가 어떤 HTML 버전을 따르는지 웹 브라우저에 알려 주는 선언문.
- 이렇게 선언하면 웹 브라우저는 웹 사이트를 표준 모드로 렌더링할지, 하위 호환을 위한 쿼크 모드로 렌더링할지를 결정한다.
    - 표준 모드
        - W3C(World Wide Web Consortium)나 WHATWG 같은 웹 표준을 최대한 정확하게 준수하여 렌더링하는 모드로, 우리가 기대하는 방식대로 CSS와 레이아웃이 적용됩니다. HTML5의 <!DOCTYPE html>은 웹 브라우저를 표준 모드로 동작하게 합니다.
    - 쿼크 모드
        - 오래전에 만들어져 현재의 표준을 따르지 않는 웹 사이트가 최신 웹 브라우저에서도 깨져 보이지 않도록 지원하는 하위 호환성 렌더링 모드입니다.
        - Doctype이 선언되지 않거나 잘 못 선언되면 웹 브라우저는 문서가 예전 방식으로 만들어졌다고 판단하여 쿼크 모드로 렌더링 한다. 이 모드에서는 CSS 해석 방식이 달라져 웹 브라우저마다 결과가 다르게 보이거나 의도와 다르게 레이아웃이 구설될 수도 있습니다.

책의 모의 면접 질문
1. Doctype 선언이 웹 브라우저의 렌더링 모드에 어떤 영향을 미칠까요?
2. 렌더링 모드에는 어떤 것이 있나요? 각각의 모드는 어떤 점이 다른가요?
3. HTML4/XHTML의 Doctype 선언과 HTML5의 Doctype 선언은 어떠 차이가 있나요? Doctype 선언이 HTML5에서 단순한 형태가 된 이유는 무엇인가요?

## head 태그 안에는 어떤 태그를 사용하나요?

- <head> 태그 : 웹사이트의 설명서, 화면에 보이지 않지만 웹 사이트의 정체성을 정의하는 메타데이터(meatadata)를 담는 공간

<title>,<meta>,<link>,<style>,<script>에 대한 내용 필요.
특히 <meta>, <link> 태그에 대해 자세히 설명해줘.


- 책의 답변
    - <head> 태그는 웹 사이트의 메타데이터를 담아 웹 브라우저와 검색 엔진에 페이지의 성격과 동작 방식을 알려주는 역할을 합니다.
    - 겉으로는 모이지 않지만 성능 최적화, 검색 엔진 최적화, 크로스 플랫폼 호환성 확보에 매우 중요합니다.
- 책의 모의 면접 질문
    - <head> 태그의 서정은 검색 엔진 최적화와 사용자 경험(UX)에 왜 중요한 영향을 미치나요?

## 시맨틱 태그란 무엇이고, 사용하는 이유를 설명할 수 있나요?

<header>, <footer>, <nav>, <main>, <article>, <section>, <aside>에 대한 내용 필요해.

- 시맨틱 태그는 이름 자체로 콘텐츠의 의미와 역할을 전달한다.
- 3가지 측면에서 중요한 역할을 한다.
    1. 검색 엔진 최적화
    2. 웹 접근성 향상
    3. 코드의 가독성과 유지보수 효율 향상


- 책의 답변
    - 시맨틱 태그는 콘텐츠의 의미를 명확히 전달하는 HTML 태그. 단순히 영역만 구분하는 <div> 태그와 달리 <header>, <main>, <article>과 같은 태그는 웹 페이지의 구조와 역할을 설명합니다.
    이 덕분에 웹 브라우저와 검색 엔진이 웹 페이지를 더 정확히 이해할 수 있습니다.
    그 결과 검색 엔진 최적화가 향상되고 스크린 리더가 구조를 쉽게 인식해 웹 접근성도 크게 개선됩니다. 또한 코드의 가독성과 유지보수성이 높아져 개발자에게도 효율적인 웹 개발 환경을 제공 합니다.

- 책의 모의 면접 질문
    1. <header>, <main>, <article>과 같은 시맨틱 태그를 사용하면 검색 엔진 최적화와 웹 접근성에 어떤 이점이 있을까요?
    2. <div> 태그 대신 시멘틱 태그를 사용하는 것이 코드 가독성과 유지보수성 측면에서 왜 더 유리할까요?


## 웹 표준과 웹 접근성이란 무엇인가요?

웹 표준은 W3C 같은 공식 표준화 기구가 제정한 웹 기술의 공식 규약입니다. 쉽게 말해 웹사이트를 만들 때 따라야 할 공통 규칙을 전 세계가 합의한 것으로, 웹의 헌법이라 말할 수 있습니다.
이런 규칙은 왜 필요할까? 바로 웹 호환성(Cross-Browsing)때문입니다.

웹 접근성(web-accessibility)은 사용자의 장애 여부나 이용 환경에 관계 없이 누구나 웹 콘텐츠를 똑같이 인식하고 사용할 수 있도록 보장하는 원칙입니다.

- 책의 답변
    - 웹 표준은 브라우저와 환경에 상관없이 동일하게 동작하는 웹을 만들기 위한 공식 규약입니다. 호환성, 유지보수성, 검색 엔진 최적화에 이점을 제공합니다.
    - 웹 접근성은 장애 유무나 환경에 관계 없이 모든 사용자가 동등하게 웹을 이용할 수 있도록 보장하는 철학으로 대체 텍스트 제공, 키보드 접근성, 색상 대비 확보 등이 핵심 요소입니다.
- 책의 모의 면접 질문
    1. 웹 표준과 웹 접근성의 차이점과 각각의 목적은 무엇인가요?
    2. 웹 전근성을 높이기 위해 HTML에서 적용할 수 있는 구체적인 방법 3가지는 무엇인가요?

## 모든 HTML 태그에는 종료 태그가 있나요?
- 책의 답변
HTML의 모든 태그가 종료 태그를 가지는 것은 아닙니다. <img>, <br>, <hr> 등과 같은 빈 태그는 자체로 완결된 요소로 내부 콘텐츠를 가질 수 없습니다.
HTML5에서는 이러한 빈 태그를 간결하게 작성하며 파서는 태그 이름 만으로 빈 태그임을 인식해 종료 태그를 기다리지 않고 즉시 돔에 추가합니다.
또한 XHTML 스타일로 슬래시(/)를 붙이는 방식은 무시되며 빈 태그는 자체로 완전한 단위로 처리되어 웹 브라우저에 바로 렌더링 됩니다.

- 책의 모의 면접 질문
    1. HTML에서 빈 태그와 일반 태그는 구조적으로 어떤 차이가 있나요? 왜 빈 태그에는 종료 태그가 필요하지 않나요?
    2. HTML5 파서는 <img>, <br>같은 빈 태그를 어떻게 처리하나요? XHTML 스타일의 슬래시(/)는 어떤 영향을 미치나요??


- 책의 답변
- 책의 모의 면접 질문