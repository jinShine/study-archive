/* =========================================================
📘 42강. Vanilla CSS 적용하기 — React에서 가장 기본적인 스타일링 방식
========================================================= */

/* -------------------------- App.css -------------------------- */
h1 {
  color: blue;
  text-align: center;
}

button {
  background-color: green;
  color: white;
  padding: 10px;
  border-radius: 5px;
}

/* -------------------------- App.js (예제 1) -------------------------- */
import "./App.css"; // App.css 불러오기

export default function App() {
  return (
    <div>
      <h1>안녕하세요, React!</h1>
      <button>클릭</button>
    </div>
  );
}

/* -------------------------- styles.css -------------------------- */
button {
  color: red;
}

/* -------------------------- App.js (예제 2) -------------------------- */
import "./styles.css"; // styles.css 불러오기

function App() {
  return (
    <div>
      <button>로그인</button>
      <button>회원가입</button>
    </div>
  );
}

export default App;

/* =========================================================
실행 결과 요약
---------------------------------------------------------
styles.css → button { color: red; }

App.js →
  <button>로그인</button>
  <button>회원가입</button>

✅ 결과:
두 버튼 모두 빨간 글씨로 출력됨 (전역 스타일 적용)
========================================================= */

/* =========================================================
핵심 정리
---------------------------------------------------------
1️⃣ Vanilla CSS는 React에서 가장 기본적인 스타일링 방식이다.
2️⃣ CSS 파일을 import하면 “전역”으로 적용되어 모든 컴포넌트에 영향을 준다.
3️⃣ 간단하지만 스타일 충돌 위험이 있다.
4️⃣ 다음 강의에서는 “CSS Modules”로 이 문제를 해결한다.
========================================================= */
