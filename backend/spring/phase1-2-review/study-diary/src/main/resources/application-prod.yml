server:
  port: 80

logging:
  level:
    root: WARN
    com.example.studydiary: INFO

# 운영에서는 Swagger 비활성화 (15번 주제)
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

file:
  upload-dir: /var/app/uploads